package main

import (
	"testing"
)

func TestMain(t *testing.T) {
	t.Parallel()
	// It would be interesting to test this but it would require launching a
	// separate process then somehow merging the coverage.
	// main()
}
